sudo: required
language: generic

services:
  - docker

before_install:
  - docker build --tag romka04/Romka04.Complex.Core.Tests -f '.\test\Core Libraries\Romka04.Complex.Core.Tests\Dockerfile' . 

script:
  - docker run romka04/Romka04.Complex.Core.Tests -- --coverage --watchAll=false
 
after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin